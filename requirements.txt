# Python >= 3.9
tortoise-orm>=0.21.0
PyYAML>=6.0

# ═══ 控制台后端（spark_console 迁移至 FastAPI，适配 Vercel serverless）═══
# API 层
fastapi>=0.116.0
# 本地开发/自托管运行（Vercel 上由平台以 ASGI 方式托管，无需 uvicorn 常驻）
uvicorn>=0.35.0
# 口令哈希 Argon2 / PII 与 cookie 的 AES-GCM 加密
argon2-cffi>=23.1.0
cryptography>=45.0.0
# 邮件（Resend HTTP，请求内联发送）
httpx>=0.28.0

# 抖音扫码登录（Playwright 连接远程 CDP 浏览器）
playwright>=1.40.0

# 计划任务 fc 分支：EventBridge 定时事件源(CreateEventSource/DeleteEventSource)
alibabacloud-eventbridge20200401>=1.0.0

# 可选数据库驱动：
# - SQLite（本地调试）：tortoise-orm 自带 aiosqlite 依赖，无需额外安装
# - PostgreSQL（生产环境 / Vercel）：统一使用 psycopg 后端
#   （Vercel 运行时会把自带的 asyncpg 覆盖到 _vendor，与 pip 安装的副本版本冲突，
#   报 connect() 的 channel_binding 参数不兼容；psycopg 无此问题）
psycopg[binary]>=3.1

# 阿里云函数计算 FC 3.0 (OpenAPI 版本 2023-03-30) Python SDK
# 安装全部三个包后 SDK 方可用；endpoint 形如 <主账号ID>.<region>.fc.aliyuncs.com
alibabacloud-fc20230330>=3.0.0

# 跨账号为用户建函数时（AssumeRole 换 STS 临时凭证）需要
alibabacloud-sts20150401>=1.0.0

# fc/provision_image_function.py 自动创建 VPC->交换机->NAT->EIP->SNAT 链路需要
alibabacloud-vpc20160428>=1.0.0
# 同脚本创建安全组（ECS API）需要
alibabacloud-ecs20140526>=1.0.0
# 同脚本自动创建 FC 服务关联角色 AliyunServiceRoleForFC（挂 VPC 必需）需要
alibabacloud-ram20150501>=1.0.0

# aliyunFC/install/ros_client.py 调用 ROS 创建/查询资源栈需要
alibabacloud-ros20190910>=1.0.0

# task 模块云端调度：阿里云 EventBridge 管控 API（默认总线定时调度 → FC，免事件费）
alibabacloud-eventbridge20200401>=2.0.1
